export const initialState = {
    item: 'Take out trash',
    completed: false,
    id: 6547658
}

export const todoReducer = (state, action) => {
    //actions: ADD_TODO, TOGGLE_TODO
    return state;
}